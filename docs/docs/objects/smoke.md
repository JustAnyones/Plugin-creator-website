# Smoke

## Example
```json
"smoke": [{
    "id": "some_id", // smoke draft ID
    "x": 0,
    "y": 0,
    "color": {}, // this is gonna be a color object
    "frames": [] // this is target frames, not the regular frames, it's an int array
}]
```
